import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic widget test placeholder — full app requires async SharedPreferences init.
    expect(true, isTrue);
  });
}
